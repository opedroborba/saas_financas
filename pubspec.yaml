name: saas_shinko
description: A new Flutter project.

publish_to: 'none' # Remove this line if you intend to publish this package.

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  supabase_flutter: ^2.5.0
  uuid: ^4.4.0
  intl: ^0.19.0
  json_annotation: ^4.9.0
  flutter_masked_text2: ^0.9.0+1
  fl_chart: 1.0.0
  pdf: ^3.10.4
  printing: ^5.12.0
  url_launcher: ^6.2.2
  path_provider: ^2.1.3
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 # OU ^5.0.0, use a versão que já estava antes, mas APENAS UMA VEZ!
  build_runner: ^2.4.9
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true